/** Host loader entry for the Usage-dashboard browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the Usage dashboard section. */
export function apply(): void {}
